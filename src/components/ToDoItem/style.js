import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  item: {
    padding: 15,
    borderColor: '#bbb',
    borderWidth: 1,
    borderStyle: 'dashed',
    borderRadius: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
    marginBottom: 10,
  },
  text: {
    fontSize: 18,
  },
});
